// src/Hero.js
import React from 'react';
import HeroSlideshow from './slideshow';

const Hero = () => {
  return (
    <section className="slideshow-container">
      <HeroSlideshow />
    </section>
  );
};

export default Hero;